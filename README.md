# K-means-and-latent-cluster-analysis
This project simulates mixtures of multivariate normal distributions and compares clustering methods. It applies k-means for different values of k, tests sensitivity to starting points, compares clusters with true labels, reports cluster statistics, and evaluates latent class clustering under different covariance and mean-separation settings.
